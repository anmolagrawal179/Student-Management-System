spring:
  application:
    name: Student-Management-System
  datasource:
    url: jdbc:mysql://localhost:3306/sms
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true